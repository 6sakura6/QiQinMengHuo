## Agent skills

### 问题追踪器

使用 GitHub Issues（仓库 `6sakura6/----`）。详见 `docs/agents/issue-tracker.md`。

### 分类标签

标准五标签词汇：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### 领域文档

单上下文布局：根目录 `CONTEXT.md` + `docs/adr/` 存放架构决策记录。详见 `docs/agents/domain.md`。
